{
  "browserType": "chromium" // Replace with your desired browser type
  "MyBookings": {
    "username": "<testusername>",
    "password": "<testpassword>" // password MUST NOT be checked in with the code. If this is on the pipeline, it will be populated with appropriate value.
  },
  "EmailService": {
    "clientId": "<testclientId>",
    "clientSecret": "<testclientSecret>" // clientSecret MUST NOT be checked in with the code. If this is on the pipeline, it will be populated with appropriate value.
  }
  }
}